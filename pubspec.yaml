name: calebshirthum
description: "A new Flutter project."
publish_to: 'none' # Prevents accidental publishing to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  get: ^4.6.6
  google_fonts: ^6.2.1
  flutter_screenutil:
  pinput:
  flutter_faq: ^1.0.1+1
  font_awesome_flutter: ^10.7.0
  flutter_rating_bar:
  d_chart: ^2.6.9
  fl_chart:
  dotted_border: ^2.1.0
  percent_indicator: ^4.2.3
  google_maps_flutter: ^2.7.0
  loading_animation_widget: ^1.2.1
  image_picker: ^1.1.2
  zoom_pinch_overlay: ^1.4.3
  get_storage: ^2.1.1
  flutter_svg:
  qr_flutter:
  flutter_launcher_icons: ^0.13.1
  device_preview: ^1.2.0
  flutter_spinkit: ^5.2.1
  fluttertoast:
  flutter_html:
  geolocator: ^9.0.0
  geocoding:
  shimmer:
  cached_network_image: ^3.4.1
  change_app_package_name: ^1.4.0
  page_transition: ^2.1.0
  google_sign_in: ^6.2.2
  intl_phone_field: ^3.2.0
  url_launcher: ^6.3.1
  table_calendar: ^3.0.9
  socket_io_client:
  firebase_messaging: ^15.1.3
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.4
  gap: ^3.0.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Add assets here
  assets:
    - assets/images/  # Include all images in the "assets/images" folder
    - assets/icons/    # Include all files in the "assets/data" folder
